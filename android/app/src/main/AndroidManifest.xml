<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Health Connect: passive steps (doc §3.4). Location/foreground-service
         permissions are intentionally NOT declared — they belong to the TURBO
         release (Phase 4, doc §2.4). -->
    <!-- ONLY what the app actually reads. HealthService requests exactly one
         data type (HealthDataType.STEPS) and only for today's window, so that
         is all we declare. Google's Health Connect policy requires each
         requested type to be used and justified; declaring unused types (or
         READ_HEALTH_DATA_IN_BACKGROUND / _HISTORY, which need strong
         justification) is a review rejection. Add a permission back the same
         day the code starts reading it — same rule as the location/
         foreground-service perms deferred to TURBO below. -->
    <uses-permission android:name="android.permission.health.READ_STEPS"/>
    <!-- Live in-app step ticker via the device pedometer. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>

    <application
        android:label="The Walk Culture"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- AdMob App ID (required — the SDK crashes on launch without it). This
             is the app-level ID; it does NOT decide test-vs-real ads — the ad
             UNIT id does (see lib/core/ad_config.dart). Safe to ship. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-8276197101210812~7737324093"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Health Connect permission-rationale (doc §3.4). -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE"/>
            </intent-filter>
        </activity>

        <!-- Points Health Connect's permission screen at the privacy policy
             (required for Google review, doc §3.4/§9). -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE"/>
                <category android:name="android.intent.category.HEALTH_PERMISSIONS"/>
            </intent-filter>
        </activity-alias>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Detect Health Connect + its permission-rationale (doc §3.4). -->
        <package android:name="com.google.android.apps.healthdata"/>
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE"/>
        </intent>
    </queries>
</manifest>
